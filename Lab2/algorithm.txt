state = 0;
    while(going)
    switch(state)
    {
        case 0:
          go forward
          start keeping track of time
          if(timeout and not hit wall)
          {
            next_state = 0;
            break;
        }else
          {
            next_state = 1;
            break;
        }
        
        case 1:
          turn right
          go straight
          start keeping track of time
          if(timeout and not hit wall)
          {
            next_state = 0;
            break;
        }else
          {
            next_state = 2;
            break;
        }
          
        case 2:
          turn right
          turn right //180
          if(timeout and not hit wall)
          {
            next_state = 0;
            break;
        }else
          {
            next_state = 3;
            break;
        }
        
        case 3:
          turn left //this is 180 from original direction
          if(timeout and not hit wall)
          {
            next_state = 0;
            break;
        }else
          {
            next_state = 1;
            break;
        }
        
    }
